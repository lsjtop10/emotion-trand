import { Typography } from "@mui/material";

export default function SettingPage() {
  return (
    <div style={{ width: "100%" }}>
      <div style={{ textAlign: "center", }}>
        <Typography
          variant="h2"
        >This is a setting page</Typography>
      </div>
    </div>
  );
}